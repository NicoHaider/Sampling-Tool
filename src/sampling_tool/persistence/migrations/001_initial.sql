-- Initial-Migration. Wird in Sprint 2 ausgefüllt (Tabellen: engagements,
-- datasets, dataset_rows, samples, sample_results, audit_events).
-- Bewusst leer gelassen, damit der Migrations-Runner schon jetzt eine
-- Versionsnummer kennt.

PRAGMA user_version = 1;
